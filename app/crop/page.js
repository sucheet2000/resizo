import CropClient from './CropClient';

export const metadata = {
    title: 'Crop Images Online Free — Image Cropper | Resizo',
    description: 'Crop images online for free. Set exact pixel coordinates or drag to select crop area. JPEG, PNG and WebP supported.',
};

export default function CropPage() {
    return <CropClient />;
}
