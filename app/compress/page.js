import CompressClient from './CompressClient';

export const metadata = {
    title: 'Compress Images Online Free — Reduce File Size | Resizo',
    description: 'Compress JPEG, PNG and WebP images online for free. Reduce file size without visible quality loss. No uploads stored, processed instantly.',
};

export default function CompressPage() {
    return <CompressClient />;
}
