import ConvertClient from './ConvertClient';

export const metadata = {
    title: 'Convert Images Online Free — JPEG PNG WebP Converter | Resizo',
    description: 'Convert images between JPEG, PNG and WebP formats online for free. Instant conversion, no account required.',
};

export default function ConvertPage() {
    return <ConvertClient />;
}
