import HeicClient from './HeicClient';

export const metadata = {
    title: 'Convert HEIC to JPEG Online Free — iPhone Photo Converter | Resizo',
    description: 'Convert HEIC and HEIF photos from iPhone to JPEG online for free. No software needed. Fast, private, instant download.',
};

export default function HeicPage() {
    return <HeicClient />;
}
